excluded:
  # SwiftPM build directory: third-party checkouts (SwiftyJSON) and build
  # artifacts. Appears once anyone runs `swift build` from the CLI.
  - "Photo Club Hub Data/.build"

  # Xcode's Core Data codegen output, committed as part of #232 step 0a. The
  # trailing underscore on raw attributes is this project's Core Data
  # convention; the colon spacing and over-long typealias names come from
  # Xcode's generator and reappear on every regeneration.
  - "Photo Club Hub Data/Sources/Photo Club Hub Data/ViewModel/CoreDataGenerated"
